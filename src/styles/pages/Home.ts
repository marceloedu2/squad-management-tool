import styled from 'styled-components'
import { Composition } from 'atomic-layout'

export const ContainerHome = styled(Composition)`
  width: 100vw;
  height: 100vh;

  display: flex;
  flex-direction: column;
  align-items: start;
`
